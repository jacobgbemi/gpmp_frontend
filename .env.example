# Base URL of the GlintPM Private Django REST API.
# Include the protocol and host, no trailing slash.
# Example (local Django dev server): http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000
